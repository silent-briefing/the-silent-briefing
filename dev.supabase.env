# Committed local defaults — copy to repo-root `.env` if you use env()-based overrides in `supabase/config.toml`.
# Current `config.toml` inlines `jwt_secret`; this file documents the same value for Clerk Dashboard → Supabase.
JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
