## Learned User Preferences

- Keep planning working files in **`docs/plans/`** (`00_task_plan.md`, `03_progress.md`, cohesive step docs; expanded roadmap in `01_expanded_silent_briefing_platform_plan.md`).
- Prefer Clerk over Supabase Auth when choosing an authentication provider.
- For product UX and information architecture, aim for Palantir-style integration and cross-referencing between related entities.
- Initial domain emphasis: United States Supreme Court (a hub for justices with supporting context and news).
- Design news and research integrations around X (Twitter) and Perplexity as primary external sources.
- Prefer self-hosted development infrastructure except where an external dependency is unavoidable.
- For Python in this repo, use `uv` (`uv add`, `uv sync`, `uv run`) rather than ad-hoc virtualenv and pip workflows.
- Prefer scaffolding for Directus and Supabase (for example `npx create-directus-extension@latest`, Supabase CLI, and Supabase MCP) instead of hand-writing boilerplate that tools generate.
- When planning, never overwrite existing plan files in `docs/plans/`; create new dated files (`YYYY-MM-DD-<feature>.md`) alongside them.
- Build a custom GUI (operator console **plus** admin interface) as the primary surface; Directus is a fallback/nice-to-have, not the way the user wants to administer the site.
- Keep source files small — target roughly ~200 LOC per file.
- Consult the in-repo `design/` assets (design system, UI kits) when doing any frontend/UX work rather than inventing styling.

## Learned Workspace Facts

- GitHub organization for this repository: `silent-briefing`.
- Local Supabase in this project commonly uses API `http://127.0.0.1:54321`, Postgres on `127.0.0.1:54322`, and Studio on `http://127.0.0.1:54323`.
- Recent Supabase CLI releases print Publishable/Secret-style keys from `supabase status` rather than legacy JWT-shaped anon/service strings; client code and environment variables may need to align with that format.
- Directus 11 connecting to local Supabase Postgres often uses the `postgres` database user for local development to avoid bootstrap issues seen with a restricted `directus_user` role; migrations may still define `directus_user` for production-like setups.
- Foundation layout includes `supabase/migrations/`, `docker-compose.yml` for Directus, `cms/` for Directus configuration and extensions/schema artifacts, and a FastAPI package under `backend/briefing/` exposing `POST /v1/intelligence/refresh` as a stub for CMS-triggered work.
- Directus loads local extensions only from immediate subfolders of `cms/extensions/`, each with its own `package.json`; nested layouts like `cms/extensions/themes/<name>` or `cms/extensions/hooks/<name>` are not discovered because the parent directory is not a valid extension package.
- Directus theme extensions declare one `appearance` each (`light` or `dark`); matching Studio mode requires a separate extension per appearance (this repo: **Silent Briefing** for light, **Silent Briefing (Dark)** for dark).
- Directus Insights starts from an empty “No Dashboards” state until you create a dashboard; custom panels (for example official intelligence refresh) are added inside a dashboard layout, not from the Insights landing page alone.
- Supabase/Directus foundation work (local CLI, migrations, Docker, hooks) is tracked task-by-task in `plans/04_foundation_supabase_directus.md` (Phase 0 there).
- Configurable **source URLs** (vote.utah, SLCO, utcourts, Ballotpedia, Google Civic) default in `backend/briefing/defaults/source_urls.py` and override via matching `UPPER_SNAKE` env vars on `Settings` (see `CLAUDE.md` Environment Variables).
- **Step 3 intelligence** — `backend/briefing/services/intelligence/`: `evidence_bundle.py`, `retrieval_stages.py` (A/B/C; Stage C can be **light** for GOP routing), `dossier_writer.py`, **`routing.py`** (`officials.subject_alignment` → stage list; staleness helper). Workers: `retrieval-pass` (`--use-routing`, `--skip-if-fresh`, `--correlate`), `dossier-write`. Env: `RETRIEVAL_MODEL`, `RETRIEVAL_STAGE_SPEC_GOP`, `RETRIEVAL_STAGE_SPEC_DEFAULT`, `RETRIEVAL_STALE_DAYS`.
- **Operator console BFF (Phase 3 prep):** FastAPI `GET /v1/console/judicial/supreme-court`, `GET /v1/console/officials/{slug}` — **service_role** server-side only; browser should use anon/authenticated Supabase client + Clerk per `docs/plans/04_foundation_supabase_directus.md`.
- Design system lives under `design/` — see `design/README.md`, `design/colors_and_type.css`, and UI kits such as `design/ui_kits/operator_console/` (reference materials for all frontend work).
- **Operator console** code lives in `console/` (Bun + Next.js App Router). Package name is `silent-briefing-console` because npm reserves the name `console`. Auth edge logic lives in **`src/proxy.ts`** (Next.js 16 renamed `middleware.ts`). Use **`bun run dev`** (webpack) if Turbopack panics with Clerk on Windows; **`bun run dev:turbo`** opt-in. Clerk: public `/sign-in`, `/sign-up`, `/api/health`; `/admin/*` requires `public_metadata.role === "admin"`; missing role defaults to **viewer** for non-admin routes. Sidebar mark: **`/branding/shield-crest.png`** (from `design/assets/shield-crest.png`, 56×56 CSS, square 1024 source).
- **Operator data queries (Phase B.3):** `console/src/lib/queries/` — Zod-validated Supabase helpers; BFF JSON for **semantic search** and **feeds** expects `POST /v1/console/search/semantic` and `GET /v1/console/feeds/{official_id}` when those routes are added.
- **Briefing home (Phase B.4):** `/` composes `console/src/components/operator/briefing/*`; KPIs from **`dossier_claims`** + **`alerts`** (org-scoped JWT) and **`GET /v1/console/briefing/intel-summary`** (Clerk Bearer + service-role **`intelligence_runs`** count and last-20 tail). **`listUtSupremeCourt`** scopes the priority list to jurisdiction **`ut`**.
- **Operator UI primitives (Phase B.2):** `console/src/components/operator/` — editorial Card, SectionHeader, MetaLabel, Portrait, SourceCite, ClaimRow, StatusDot, KpiTile, EmptyState. Local dev showcase: **`/operator-primitives`** (404 in production).
- **Operator Supabase RLS (Phase B.1):** `dossier_claims` exposes only **`published`** claims in stages **`writer_sonar`** / **`human_edit`** to **`authenticated`**; **`opinions`**, **`bills`**, **`media_coverage`** are readable when **`published = true`**. Migration: `supabase/migrations/20260424103000_operator_surface_rls.sql`.
- **GUI Phase A (foundation) is complete:** Clerk Orgs + three roles, hybrid Supabase client + FastAPI BFF (`/v1/console/*`, `/v1/admin/*`), themed shadcn primitives, React Query + Zod BFF client. **CI:** `.github/workflows/gui-ci.yml` — Bun typecheck/lint, Vitest, `check:secrets`, production build, Playwright + axe, Lighthouse (warn-only job), backend pytest. **Local parity:** `cd console && bun run typecheck && bun run lint && bun run test && bun run build`; E2E needs the app on **127.0.0.1:3000** (`bun run dev` or `start`) unless you mirror CI with `CI=true` after `build` on a free port.
- **Judicial Watch (Phase B.5):** Operator nav **Judicial** → `/judicial` (landing); rosters at `/judicial/supreme-court`, `/judicial/court-of-appeals`, `/judicial/district` use **`listUtByOfficeType`** (jurisdiction `ut`); cards link to **`/judicial/[slug]`**.
- **Dossier shell (Phase B.6 partial):** Shared **`DossierView`** for **`/judicial/[slug]`** and **`/officials/[slug]`** — navy gradient header, six tabs (Overview / Claims / Adversarial / Graph / Feed / Timeline). **Overview**: roster **`OfficialCardRow`** + jurisdiction label (bio, key facts). **Claims** / **Timeline**: published **`dossier_claims`**. **Adversarial**: flagged claims via **`getAdversarialFlags`**, split view (groundedness + critique/synthesis from metadata). **Graph** / **Feed** still placeholders; full B.6 Playwright tab sweep still to do.