## Learned User Preferences

- Keep planning working files in **`docs/plans/`** (`00_task_plan.md`, `03_progress.md`, cohesive step docs; expanded roadmap in `01_expanded_silent_briefing_platform_plan.md`).
- Prefer Clerk over Supabase Auth when choosing an authentication provider.
- For product UX and information architecture, aim for Palantir-style integration and cross-referencing between related entities.
- Initial domain emphasis: United States Supreme Court (a hub for justices with supporting context and news).
- Design news and research integrations around X (Twitter) and Perplexity as primary external sources.
- Prefer self-hosted development infrastructure except where an external dependency is unavoidable.
- For Python in this repo, use `uv` (`uv add`, `uv sync`, `uv run`) rather than ad-hoc virtualenv and pip workflows.
- Prefer scaffolding for Directus and Supabase (for example `npx create-directus-extension@latest`, Supabase CLI, and Supabase MCP) instead of hand-writing boilerplate that tools generate.
- When planning, never overwrite existing plan files in `docs/plans/`; create new dated files (`YYYY-MM-DD-<feature>.md`) alongside them.
- Build a custom GUI (operator console **plus** admin interface) as the primary surface; Directus is a fallback/nice-to-have, not the way the user wants to administer the site.
- Keep source files small — target roughly ~200 LOC per file.
- Consult the in-repo `design/` assets (design system, UI kits) when doing any frontend/UX work rather than inventing styling.

## Learned Workspace Facts

- GitHub organization for this repository: `silent-briefing`.
- Local Supabase commonly uses API `http://127.0.0.1:54321`, Postgres on `127.0.0.1:54322`, and Studio on `http://127.0.0.1:54323`; recent CLI output may show Publishable/Secret-style keys while the local demo JWT anon key remains valid for `supabase-js`.
- **Console Supabase env:** `console/src/lib/supabase/public-env.ts` — `getPublicSupabaseUrl()` and `getPublicSupabaseAnonKey()`; in **development** and **test**, missing `NEXT_PUBLIC_SUPABASE_*` falls back to `supabase start` defaults; **production** must set them explicitly.
- **Console server Supabase + Clerk:** `createServerSupabaseClient()` uses `@supabase/supabase-js` `createClient` with async `accessToken` from `getClerkSupabaseAccessToken` (not `@supabase/ssr` `createServerClient`) so Clerk third-party tokens do not hit the SSR helper’s `onAuthStateChange` path.
- Directus 11 local dev often uses Postgres user `postgres`; extensions load only from immediate subfolders of `cms/extensions/` (each with its own `package.json`); theme extensions declare one `appearance` each; Directus Insights needs a dashboard before custom panels apply.
- Foundation layout: `supabase/migrations/`, `docker-compose.yml` for Directus, `cms/` for Directus config and extensions, FastAPI under `backend/briefing/` with `POST /v1/intelligence/refresh`; configurable source URLs default in `backend/briefing/defaults/source_urls.py` with `UPPER_SNAKE` env overrides on `Settings` (see `CLAUDE.md`).
- **Step 3 intelligence** — `backend/briefing/services/intelligence/`: `evidence_bundle.py`, `retrieval_stages.py`, `dossier_writer.py`, **`routing.py`**; workers `retrieval-pass`, `dossier-write`; env `RETRIEVAL_MODEL`, stage specs, `RETRIEVAL_STALE_DAYS`.
- **Operator console BFF:** FastAPI `GET /v1/console/judicial/supreme-court`, `GET /v1/console/officials/{slug}`, briefing intel summary, `GET /v1/feeds/{official_id}` (X + Perplexity, `FEED_CACHE_SECONDS` default 600, `X_API_BEARER_TOKEN` optional) — **service_role** server-side; browser uses Supabase with Clerk JWT per `docs/plans/04_foundation_supabase_directus.md`.
- Design system lives under `design/` — `design/README.md`, `design/colors_and_type.css`, UI kits such as `design/ui_kits/operator_console/`.
- **Operator console** in `console/` (Bun + Next.js App Router, package `silent-briefing-console`): auth edge in **`src/proxy.ts`**; **`bun run dev`** defaults to webpack; **`bun run dev:turbo`** optional; Clerk public `/sign-in`, `/sign-up`, `/api/health`; `/admin/*` requires `public_metadata.role === "admin"`; sidebar mark **`/branding/shield-crest.png`** — use `next/image` with sufficient `quality` / `sizes` so fine detail is not lost when scaling down.
- **Operator surfaces:** Zod helpers in `console/src/lib/queries/`; briefing home composes `operator/briefing/*` with `dossier_claims`, `alerts`, and BFF intel summary; Judicial under `/judicial/*` (`listUtByOfficeType`, jurisdiction `ut`); **Officials hub** `/officials` — filters in query string (`j`, `office`, `party`, `align`, `current`), virtualized roster, row → `/officials/[slug]`, save view → `user_saved_views` (needs Clerk `supabase` JWT for authenticated insert); **Saved** `/saved` lists the signed-in user’s `user_saved_views` for the active workspace (`organization.id` or `personal_${userId}`), with remove; **Alerts** `/alerts` plus top-bar **`AlertsBell`** — reads `alerts` for the **active Clerk organization** only (RLS: JWT `org_id` must match row `org_id`); mark-read updates `read_at`. **Worker → alerts:** `backend/briefing/services/alerts/dispatcher.py` inserts via service role when `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and **`ALERTS_DEFAULT_ORG_ID`** (Clerk org id) are set; otherwise `AlertDispatcher.try_from_settings` returns `None` and jobs skip alerting. **Compare** `/compare` — `?s=slug1,slug2` (2–4 officials), command palette (`ComparePicker`), `CompareMatrix` summaries, gold inset + label when a pair has a **direct** accepted `entity_edge`, print CSS `console/src/styles/compare-print.css`; **Global search** `/search` — lexical queries in `console/src/lib/search/lexical.ts` (officials + published bills/opinions/media), optional semantic via BFF `POST /v1/search/semantic` (`backend/briefing/api/routes/search.py`, Perplexity embed + `match_rag_chunks_public`); **⌘K** palette `GlobalSearchPalette` in operator shell; disable client semantic with `NEXT_PUBLIC_SEARCH_SEMANTIC=0`; shared **`DossierView`** for `/judicial/[slug]` and `/officials/[slug]` (header **Bookmark** → `user_saved_views` kind `dossier`); dev-only **`/operator-primitives`**; RLS in `supabase/migrations/20260424103000_operator_surface_rls.sql`.
- **GUI CI:** `.github/workflows/gui-ci.yml` — Bun typecheck/lint, Vitest, `check:secrets`, production build, Playwright + axe, Lighthouse warn-only, backend pytest; local parity: `cd console && bun run typecheck && bun run lint && bun run test && bun run build`.
