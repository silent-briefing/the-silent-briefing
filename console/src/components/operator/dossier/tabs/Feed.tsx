"use client";

export function FeedPanel() {
  return (
    <p className="font-sans text-sm text-[var(--fg-3)]">
      News and feed items will load via BFF <code className="font-mono text-[13px]">/v1/console/feeds/…</code>.
    </p>
  );
}
