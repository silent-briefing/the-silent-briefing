# Repo root — optional. Supabase CLI loads `.env` when `config.toml` uses env().
# Local `jwt_secret` is inlined in `supabase/config.toml`; see `dev.supabase.env` for the same value.
# Do not commit a real `.env`.
JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
